# Copy to .env and fill in. Either a token (preferred) or username/password.
# splunk-extract reads .env from the current directory; blank values are ignored.
SPLUNK_URL=https://127.0.0.1:8089
SPLUNK_TOKEN=
SPLUNK_USERNAME=
SPLUNK_PASSWORD=
